# Chat KB Demo

## 快速开始

```bash
# 安装依赖
pnpm install

# 启动开发服务器
pnpm dev

# 构建生产版本
pnpm build
```

## API 配置

后端接口地址通过环境变量配置（`CHAT_` 前缀，详见 ./env.example）。

接口规范详见 [docs/API接口说明.md](./docs/API接口说明.md)。
